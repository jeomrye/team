<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.std.mapper.UserMapper">

<!-- 회원가입 -->


<insert id="insertMem">

insert into 
member
(uno,userid,memberno,username, password, email,phone, gender, regdate, updatedate,mileage,coupon, companynumber) 
values 
((uno_seq.nextval),#{userid},#{memberno},#{username},#{password},#{email},#{phone},#{gender},sysdate,sysdate,0,null,#{companynumber}) 

</insert>

<!-- 아이디 중복검사 -->


<select id="idCheck" resultType="int">
select 
count(*) 
from 
member 
where userid = #{userid} 
</select>

<!-- 사업자등록번호 검사 -->


<select id="companyNumCheck" resultType="int">
select 
count(*) 
from 
company 
where companynumber = #{companynumber} 
</select>



</mapper>